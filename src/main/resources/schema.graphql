
schema {
    query: Query
}

type Query {
    getAllEmployees: [Employee]
    getEmployee(id: Int): Employee
}

type Employee {
    id: Int
    firstName: String
    lastName: String
    jobTitle: String
    salary: Int
}