
schema {
    query: Query
}

type Query {
    getAllEmployees: [Employee]
    getEmployee(id: Int): Employee
}

type Employee {
    id: Int,
    first_name: String
    last_name: String
    job_title: String
    salary: Int
}